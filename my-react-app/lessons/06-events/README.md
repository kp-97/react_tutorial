# 06 - Events

Pass functions to event props. TypeScript infers inline events; named handlers can
use types such as `ChangeEvent<HTMLInputElement>`. Use an arrow wrapper for arguments.

Exercise: identify which lesson button was selected. Done when handlers are typed
and run only after accessible interaction.
