# 11 — State design and reducers

Keep state minimal, lift shared state to the closest common parent, and use `useReducer` for related transitions. Reducers are pure and immutable. Exercise: add decrement/reset actions and test the reducer.
