# 14 — Data fetching

Model loading, error, empty, and success states. Cancel stale requests and keep API code behind a boundary. Exercise: fetch an endpoint with retry. Done when all network outcomes have visible UI.
