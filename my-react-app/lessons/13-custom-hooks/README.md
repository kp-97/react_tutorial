# 13 - Custom hooks

A `use`-prefixed function composes hooks to share behavior, not state instances.
Type parameters and annotate tuples such as `[boolean, () => void]` when needed.

Exercise: create a typed `useDocumentTitle` with cleanup. Done when hook lint rules pass.
