# 13 — Custom hooks

A `use`-prefixed function composes hooks to share behavior, not state instances. Exercise: create `useDocumentTitle` with cleanup. Done when hook lint rules pass and its API is small.
