# 16 — Styling and assets

Import CSS beside components, use classes for variants and custom properties for tokens. Imported assets are fingerprinted; `public` paths remain unchanged. Exercise: create a responsive card grid with light/dark variants.
