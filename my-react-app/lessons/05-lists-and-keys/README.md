# 05 — Lists and keys

Render arrays with `map`. Stable data keys let React preserve identity; avoid indexes when items reorder. Exercise: filter and sort without mutation. Done when there are no key warnings.
