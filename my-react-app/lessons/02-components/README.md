# 02 — Components

A component is a capitalized function that returns UI. Export components from their
modules and compose small components into larger ones. Call them with JSX, never as
ordinary functions when they use hooks.

Exercise: extract the paragraph into a `LessonSummary` component in its own file.
Done when imports/exports work and each component has one clear responsibility.
